<Window x:Class="DocViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:panel="clr-namespace:DocViewer.Controls" 
        xmlns:local1="clr-namespace:DocViewer.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <local1:PanelControlPageViewModel x:Key="ABC" />
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
                <RowDefinition Height="0.80*"/>
                <RowDefinition Height="0.20*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="0*"/>
            </Grid.ColumnDefinitions>
        
        <!-- 
            ToDo: Add control with jpg document. Put it as control component.  
        <!-->
        <panel:ImgControl Grid.Row="0" DataContext="{Binding Source={StaticResource ABC}}"/>
        <panel:PanelControl Grid.Row="1" DataContext="{Binding Source={StaticResource ABC}}"/>
    </Grid>
</Window>
